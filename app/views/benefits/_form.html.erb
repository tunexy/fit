<%= form_for(@benefit) do |f| %>
  <% if @benefit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@benefit.errors.count, "error") %> prohibited this benefit from being saved:</h2>

      <ul>
      <% @benefit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :support %><br />
	<%= f.select :support, ["Protein", "FatControl", "FatBurner", "Pre-WorkOut", "DuringWorkOut", "Creatine"], :include_blank => :true %>
  </div>
  <div class="field">
    <%= f.label :purpose %><br />
    <%= f.select :purpose, ["Build Muscle", "Lose Fat", "Improve Sport", "Improve WorkOut", "Health $ Fitness"], :include_blank => :true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
